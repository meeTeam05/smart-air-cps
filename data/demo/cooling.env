# Class 2 — Cooling / ventilation: bật relay_1 (fan) + relay_2 (light)
# delta_temp >= 8 → temp = baseline 32.80 + 8 = 40.8, dùng 42
# delta_hum < 0  → hum < baseline 59, dùng 45
LABEL="Class 2 — Cooling / ventilation (fan + light)"
DEVICE_ID="dc:b4:d9:13:ed:8c"
TEMPERATURE=42.0
HUMIDITY=45.0
CO_PPM=2.0
NO2_PPM=0.02
